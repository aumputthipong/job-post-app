import { ScreenPlaceholder } from "@/components/screen-placeholder";

// Real form + Firebase sign-in lands in step 4.2.
export default function Login() {
  return <ScreenPlaceholder title="เข้าสู่ระบบ" note="ฟอร์ม + Firebase Auth มาใน Phase 4.2" />;
}
